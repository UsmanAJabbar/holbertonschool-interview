#ifndef _SUBSTRING_H
#define _SUBSTRING_H

#include <stdlib.h>
#include <stdio.h>

int *find_substring(char const *s, char const **words, int nb_words, int *n);
int str_search(char const *s, char const *word);

#endif
